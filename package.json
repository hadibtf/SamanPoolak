{
  "name": "business-platform-react",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@fortawesome/fontawesome-free": "^7.2.0",
    "@testing-library/dom": "^10.4.1",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^13.5.0",
    "dexie": "^4.4.4",
    "dexie-react-hooks": "^4.4.0",
    "html2canvas": "^1.4.1",
    "jspdf": "^4.2.1",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "react-element-popper": "^2.1.7",
    "react-multi-date-picker": "^4.5.2",
    "react-router-dom": "^7.18.0",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy:web": "node scripts/deploy.mjs web --mode=automatic",
    "deploy:platform": "node scripts/deploy.mjs platform --mode=automatic",
    "deploy:api": "node scripts/deploy.mjs api --mode=automatic",
    "deploy:landing": "node scripts/deploy.mjs landing --mode=automatic",
    "deploy:jobs": "node scripts/deploy.mjs jobs --mode=automatic",
    "deploy:employee": "node scripts/deploy.mjs employee --mode=automatic",
    "deploy:all": "node scripts/deploy.mjs all --mode=automatic",
    "deploy:web:manual": "node scripts/deploy.mjs web --mode=manual",
    "deploy:platform:manual": "node scripts/deploy.mjs platform --mode=manual",
    "deploy:api:manual": "node scripts/deploy.mjs api --mode=manual",
    "deploy:landing:manual": "node scripts/deploy.mjs landing --mode=manual",
    "deploy:jobs:manual": "node scripts/deploy.mjs jobs --mode=manual",
    "deploy:employee:manual": "node scripts/deploy.mjs employee --mode=manual",
    "deploy:all:manual": "node scripts/deploy.mjs all --mode=manual",
    "export:attendance": "node scripts/export-attendance.mjs"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
